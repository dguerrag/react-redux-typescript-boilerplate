export enum CssVariables {
	CARD_WIDTH = '--card-width',
	CARD_HEIGHT = '--card-height',
	CARD_DOWN_INFO_HEIGHT = '--card-down-info-height',
	CARD_MARGIN = '--card-margin',
	HORIZONTAL_PADDING = '--horizontal-padding'
}